# Database Configuration
POSTGRES_USER=oposiciones
POSTGRES_PASSWORD=change-this-password-in-production
POSTGRES_DB=oposiciones_flashcards
POSTGRES_PORT=5435

# Redis Configuration
REDIS_PASSWORD=change-this-password-in-production
REDIS_PORT=6380

# pgAdmin Configuration
PGADMIN_EMAIL=admin@oposiciones.local
PGADMIN_PASSWORD=change-this-password-in-production
PGADMIN_PORT=5050

# Backend Configuration (copiar a backend/.env)
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:${POSTGRES_PORT}/${POSTGRES_DB}
REDIS_URL=redis://localhost:${REDIS_PORT}
SECRET_KEY=generate-a-random-secret-key-here
